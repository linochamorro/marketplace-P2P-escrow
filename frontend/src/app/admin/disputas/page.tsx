import { DisputasAdmin } from '../AdminUI';

/**
 * @file admin/disputas/page.tsx
 * @description Ruta `/admin/disputas` integrada con la lectura real de disputas.
 */

/**
 * Monta la lista real y las acciones binarias de disputas.
 *
 * @returns resolución binaria de disputas de Story 9
 */
export default function DisputasPage() {
  return <DisputasAdmin />;
}
