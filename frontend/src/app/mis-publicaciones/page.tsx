import GestionPublicacionesPropias from '../GestionPublicacionesPropias';

/**
 * Ruta de gestión inline de las publicaciones del vendedor autenticado.
 *
 * @returns contenedor cliente conectado a categorías y publicaciones propias
 */
export default function MisPublicacionesPage() {
  return <GestionPublicacionesPropias />;
}
